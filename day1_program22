Alice in wonderland:

#include <stdio.h>
#include <string.h>
#include <math.h>
#include <stdlib.h>

#include <stdio.h>

int main() {
    int num, si1, si2, sum;

    scanf("%d", &num);

    si1 = num / 10;      
    si2 = num % 10;       
    sum = si1 + si2;

    printf("Bird said:%d\n",num);
    printf("Alice must go in path-%d\n", sum);

    return 0;
}da
